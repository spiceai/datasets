name: spiceai.datasets.holesky_beacon_recent_voluntary_exits
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.holesky_beacon_recent_voluntary_exits AS (
        SELECT * FROM spiceai.datasets.internal_holesky_beacon_recent_voluntary_exits
      )
