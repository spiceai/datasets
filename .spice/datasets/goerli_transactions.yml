name: spiceai.datasets.goerli_transactions
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.goerli_transactions AS (
        SELECT * FROM spiceai.datasets.internal_goerli_transactions
      )
