name: spiceai.datasets.polygon_asset_transfers
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.polygon_asset_transfers AS (
        SELECT * FROM spiceai.datasets.internal_polygon_asset_transfers
      )
