name: spiceai.datasets.goerli_withdrawals
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.goerli_withdrawals AS (
        SELECT * FROM spiceai.datasets.internal_goerli_withdrawals
      )
