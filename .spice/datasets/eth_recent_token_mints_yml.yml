name: spiceai.datasets.eth_recent_token_mints_yml
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eth_recent_token_mints_yml AS (
        SELECT * FROM spiceai.datasets.internal_eth_recent_token_mints_yml
      )
