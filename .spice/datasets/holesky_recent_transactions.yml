name: spiceai.datasets.holesky_recent_transactions
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.holesky_recent_transactions AS (
        SELECT * FROM spiceai.datasets.internal_holesky_recent_transactions
      )
