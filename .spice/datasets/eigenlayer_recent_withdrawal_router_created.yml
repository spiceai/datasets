name: spiceai.datasets.eigenlayer_recent_withdrawal_router_created
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eigenlayer_recent_withdrawal_router_created AS (
        SELECT * FROM spiceai.datasets.internal_eigenlayer_recent_withdrawal_router_created
      )
