name: spiceai.datasets.eigenlayer_strategy_manager_share_withdrawal_queued
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eigenlayer_strategy_manager_share_withdrawal_queued AS (
        SELECT * FROM eth.eigenlayer.strategy_manager_share_withdrawal_queued
      )
