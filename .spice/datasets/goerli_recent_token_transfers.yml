name: spiceai.datasets.goerli_recent_token_transfers
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.goerli_recent_token_transfers AS (
        SELECT * FROM spiceai.datasets.internal_goerli_recent_token_transfers
      )
