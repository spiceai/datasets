name: spiceai.datasets.holesky_contracts
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.holesky_contracts AS (
        SELECT * FROM spiceai.datasets.internal_holesky_contracts
      )
