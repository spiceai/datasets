name: spiceai.datasets.eth_uniswap_v2_event_swaps
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eth_uniswap_v2_event_swaps AS (
        SELECT * FROM spiceai.datasets.internal_eth_uniswap_v2_event_swaps
      )
