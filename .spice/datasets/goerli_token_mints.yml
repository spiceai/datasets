name: spiceai.datasets.goerli_token_mints
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.goerli_token_mints AS (
        SELECT * FROM spiceai.datasets.internal_goerli_token_mints
      )
