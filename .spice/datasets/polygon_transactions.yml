name: spiceai.datasets.polygon_transactions
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.polygon_transactions AS (
        SELECT * FROM spiceai.datasets.internal_polygon_transactions
      )
