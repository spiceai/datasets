name: spiceai.datasets.eigenlayer_withdrawal_router_created
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eigenlayer_withdrawal_router_created AS (
        SELECT * FROM eth.eigenlayer.withdrawal_router_created
      )
