name: spiceai.datasets.goerli_token_transfers_with_tx
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.goerli_token_transfers_with_tx AS (
        SELECT * FROM spiceai.datasets.internal_goerli_token_transfers_with_tx
      )
