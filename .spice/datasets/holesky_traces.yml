name: spiceai.datasets.holesky_traces
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.holesky_traces AS (
        SELECT * FROM spiceai.datasets.internal_holesky_traces
      )
