name: spiceai.datasets.goerli_blocks
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.goerli_blocks AS (
        SELECT * FROM spiceai.datasets.internal_goerli_blocks
      )
