name: spiceai.datasets.eigenlayer_eigenpod_balances
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eigenlayer_eigenpod_balances AS (
        SELECT * FROM eth.eigenlayer.eigenpod_balances
      )
