name: spiceai.datasets.eigenlayer_recent_eigenpod_balances
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eigenlayer_recent_eigenpod_balances AS (
        SELECT * FROM eth.eigenlayer.recent_eigenpod_balances
      )
