name: spiceai.datasets.goerli_traces
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.goerli_traces AS (
        SELECT * FROM spiceai.datasets.internal_goerli_traces
      )
