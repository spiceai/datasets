name: spiceai.datasets.goerli_beacon_proposer_slashings
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.goerli_beacon_proposer_slashings AS (
        SELECT * FROM spiceai.datasets.internal_goerli_beacon_proposer_slashings
      )
