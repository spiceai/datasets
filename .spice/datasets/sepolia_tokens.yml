name: spiceai.datasets.sepolia_tokens
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.sepolia_tokens AS (
        SELECT * FROM spiceai.datasets.internal_sepolia_tokens
      )
