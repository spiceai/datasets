name: spiceai.datasets.holesky_beacon_slots
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.holesky_beacon_slots AS (
        SELECT * FROM spiceai.datasets.internal_holesky_beacon_slots
      )
