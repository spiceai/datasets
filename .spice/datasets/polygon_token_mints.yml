name: spiceai.datasets.polygon_token_mints
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.polygon_token_mints AS (
        SELECT * FROM spiceai.datasets.internal_polygon_token_mints
      )
