name: spiceai.datasets.eigenlayer_strategy_manager_withdrawal_completed
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eigenlayer_strategy_manager_withdrawal_completed AS (
        SELECT * FROM spiceai.datasets.internal_eigenlayer_strategy_manager_withdrawal_completed
      )
