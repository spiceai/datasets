name: spiceai.datasets.goerli_tokens
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.goerli_tokens AS (
        SELECT * FROM spiceai.datasets.internal_goerli_tokens
      )
