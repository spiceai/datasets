name: spiceai.datasets.eth_beacon_bls_to_execution_changes
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eth_beacon_bls_to_execution_changes AS (
        SELECT * FROM spiceai.datasets.internal_eth_beacon_bls_to_execution_changes
      )
