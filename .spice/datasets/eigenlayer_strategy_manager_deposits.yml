name: spiceai.datasets.eigenlayer_strategy_manager_deposits
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eigenlayer_strategy_manager_deposits AS (
        SELECT * FROM eth.eigenlayer.strategy_manager_deposits
      )
