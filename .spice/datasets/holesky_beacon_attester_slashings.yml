name: spiceai.datasets.holesky_beacon_attester_slashings
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.holesky_beacon_attester_slashings AS (
        SELECT * FROM spiceai.datasets.internal_holesky_beacon_attester_slashings
      )
