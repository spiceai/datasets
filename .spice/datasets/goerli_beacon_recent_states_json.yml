name: spiceai.datasets.goerli_beacon_recent_states_json
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.goerli_beacon_recent_states_json AS (
        SELECT * FROM spiceai.datasets.internal_goerli_beacon_recent_states_json
      )
