name: spiceai.datasets.eigenlayer_strategy_manager_added_to_deposit_whitelist
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eigenlayer_strategy_manager_added_to_deposit_whitelist AS (
        SELECT * FROM eth.eigenlayer.strategy_manager_added_to_deposit_whitelist
      )
