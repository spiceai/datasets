name: spiceai.datasets.goerli_beacon_recent_slots
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.goerli_beacon_recent_slots AS (
        SELECT * FROM spiceai.datasets.internal_goerli_beacon_recent_slots
      )
