name: spiceai.datasets.eth_blocks
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eth_blocks AS (
        SELECT * FROM spiceai.datasets.internal_eth_blocks
      )
