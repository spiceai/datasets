name: spiceai.datasets.btc_transaction_inputs
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.btc_transaction_inputs AS (
        SELECT * FROM spiceai.datasets.internal_btc_transaction_inputs
      )
