name: spiceai.datasets.ens_domains
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.ens_domains AS (
        SELECT * FROM spiceai.datasets.internal_ens_domains
      )
