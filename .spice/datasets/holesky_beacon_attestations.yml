name: spiceai.datasets.holesky_beacon_attestations
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.holesky_beacon_attestations AS (
        SELECT * FROM spiceai.datasets.internal_holesky_beacon_attestations
      )
