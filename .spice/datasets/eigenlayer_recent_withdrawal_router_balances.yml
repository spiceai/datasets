name: spiceai.datasets.eigenlayer_recent_withdrawal_router_balances
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eigenlayer_recent_withdrawal_router_balances AS (
        SELECT * FROM eth.eigenlayer.recent_withdrawal_router_balances
      )
