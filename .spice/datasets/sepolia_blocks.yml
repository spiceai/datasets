name: spiceai.datasets.sepolia_blocks
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.sepolia_blocks AS (
        SELECT * FROM spiceai.datasets.internal_sepolia_blocks
      )
