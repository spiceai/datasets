name: spiceai.datasets.goerli_erc1155_tokens
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.goerli_erc1155_tokens AS (
        SELECT address, name, symbol, decimals, total_supply, block_number, block_timestamp, block_hash
        FROM goerli.tokens
        WHERE is_erc1155 = true
      )