name: spiceai.datasets.goerli_beacon_bls_to_execution_changes
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.goerli_beacon_bls_to_execution_changes AS (
        SELECT * FROM spiceai.datasets.internal_goerli_beacon_bls_to_execution_changes
      )
