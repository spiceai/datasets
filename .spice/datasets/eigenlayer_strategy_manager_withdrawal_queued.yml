name: spiceai.datasets.eigenlayer_strategy_manager_withdrawal_queued
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eigenlayer_strategy_manager_withdrawal_queued AS (
        SELECT * FROM spiceai.datasets.internal_eigenlayer_strategy_manager_withdrawal_queued
      )
