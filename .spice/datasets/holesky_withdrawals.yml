name: spiceai.datasets.holesky_withdrawals
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.holesky_withdrawals AS (
        SELECT * FROM spiceai.datasets.internal_holesky_withdrawals
      )
