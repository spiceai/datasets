name: spiceai.datasets.eth_beacon_states_json
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eth_beacon_states_json AS (
        SELECT * FROM spiceai.datasets.internal_eth_beacon_states_json
      )
