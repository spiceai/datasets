name: spiceai.datasets.sepolia_recent_traces
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.sepolia_recent_traces AS (
        SELECT * FROM spiceai.datasets.internal_sepolia_recent_traces
      )
