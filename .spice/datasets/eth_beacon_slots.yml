name: spiceai.datasets.eth_beacon_slots
type: view
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW spiceai.datasets.eth_beacon_slots AS (
        SELECT * FROM spiceai.datasets.internal_eth_beacon_slots
      )
